gpus: 1
accelerator: "cuda" # use ddp for gpus > 1. Also see PyTorch Lightning documentation on distributed training.
workers: 16 # I recommend tuning this parameter for faster data augmentation processing
dataset_dir: "D:\\Assignment\\dance\\Unsupervised-Rhythm-Clustering-Embedding\\data\\GZTAN"
dataset: "GTZAN"

# train options
seed: 42
batch_size: 128
max_epochs: 200

# SimCLR model options
projection_dim: 64 # Projection dim. of SimCLR projector

# loss options
k: 0.5
optimizer: "Adam" # or LARS (experimental)
learning_rate: 3.0e-4
weight_decay: 1.0e-6 # "optimized using LARS [...] and weight decay of 10−6"
temperature: 0.5 # see appendix B.7.: Optimal temperature under different batch sizes

# reload options
checkpoint_path: "" 

# learning options
num_clusters: 10
hidden_size: 32
alpha: 0.1

# audio data augmentation options
audio_length: 127840
sample_rate: 16000
transforms_polarity: 0.8
transforms_noise: 0.01
transforms_gain: 0.3
transforms_filters: 0.8
transforms_delay: 0.3
transforms_pitch: 0.6
transforms_reverb: 0.6
load_dataset: True
